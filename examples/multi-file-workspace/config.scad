// Shared configuration values
// This file is included via `include <config.scad>` so all
// variables are available directly in the including file.

// Part dimensions
part_size = 15;
wall_thickness = 2;

// Quality — keep low for fast rendering
$fn = 16;
